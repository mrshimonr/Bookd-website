import {createClient} from "@supabase/supabase-js";import Link from "next/link";import {notFound} from "next/navigation";import {CalendarDays,Clock} from "lucide-react";
export default async function PublicBusiness({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const supabase=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!);const{data:business}=await supabase.from("businesses").select("id,name,description,theme").eq("slug",slug).eq("published",true).maybeSingle();if(!business)notFound();const{data:services}=await supabase.from("services").select("id,name,description,duration_minutes,price_cents").eq("business_id",business.id).eq("active",true);const color=(business.theme as {color?:string})?.color||"#4f46e5";return <main className="public-site" style={{"--business":color} as React.CSSProperties}><nav><Link href="/" className="brand"><span className="public-mark">{business.name[0]}</span>{business.name}</Link><a href="#services" className="btn public-btn">Book now</a></nav><section className="public-hero"><span>WELCOME TO</span><h1>{business.name}</h1><p>{business.description||"Professional service with simple online booking."}</p><a href="#services" className="btn public-btn">Choose a service</a></section><section className="public-services" id="services"><span className="eyebrow">Book online</span><h2>Choose a service</h2><div>{services?.map(s=><article key={s.id}><div><h3>{s.name}</h3><p>{s.description||"Book this service at a time that works for you."}</p><span><Clock size={15}/>{s.duration_minutes} minutes</span></div><aside><b>${(s.price_cents/100).toFixed(2)}</b><button><CalendarDays size={16}/> Select</button></aside></article>)}</div></section><footer>Powered by <b>Bookd</b></footer></main>}
